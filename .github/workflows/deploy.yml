name: Deploy
on:
  push:
    branches: [master]

jobs:
  deployment:
    runs-on: amazon-linux-latest
    environment: production
    steps:
      - name: deploy
        script: 
            cd ~/server
            git pull origin master
            git status
            npm i
            npm run start:dev